query {
    versionsCleanerConfig {
        disabled
        cronExpression
        nbVersionsToKeep
        deleteOrphanedVersions
        checkIntegrity
        reindexDefaultWorkspace
        maxExecutionTimeInMs
    }
}
